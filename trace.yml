---

- hosts: 192.168.1.105
  gather_facts: no

vars:
  cli:
    host: “{{ inventory_hostname }}”
    username: admin
    password: 1qaz!QAZ

tasks:
  - name: run show version
    ios_command: 
      commands: show version
      provider: “{{ cli }}”
